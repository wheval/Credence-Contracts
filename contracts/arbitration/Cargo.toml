[package]
name = "credence_arbitration"
version = "0.1.0"
edition = "2021"
description = "Credence arbitration voting system — weighted voting for dispute resolution"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "22.0", features = ["testutils"] }
