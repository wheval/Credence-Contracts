[package]
name = "credence_bond"
version = "0.1.0"
edition = "2021"
description = "Credence identity bond contract — lock USDC, track duration, slashing"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "22.0", features = ["testutils"] }
